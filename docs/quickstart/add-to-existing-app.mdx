---
title: Add Frac to existing app
sidebarTitle: "Overview"
description: "Add Frac packages to your existing MCP server or React app for view support and type inference."
---

Frac contains 2 main packages that can be used together or independently.

## The packages

- **`frac/server`**: A drop-in replacement for the official MCP SDK that adds view registration and type inference capabilities.
- **`frac/web`**: A React library providing hooks, components, and the runtime glue to render your views inside the host's iframe environment.

## Option 1: use `frac/server` (Recommended)

<Card title="Use frac/server" icon="server" href="/quickstart/add-to-existing-app/server">
  **Best if**: you're using the official TypeScript MCP SDK and want to add rich UI views with full type safety.
</Card>

## Option 2: use `frac/web` only

<Card title="Use frac/web only" icon="browser" href="/quickstart/add-to-existing-app/web">
  **Best if**: you already have an MCP server in a non-TypeScript runtime and only want React hooks for UI components.
</Card>
